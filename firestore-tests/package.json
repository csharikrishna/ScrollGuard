{
  "name": "scrollguard-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore Security Rules emulator test suite for ScrollGuard's parental-control feature (spec Part E.3 / Part H allow-deny matrix).",
  "scripts": {
    "test": "mocha test/ --timeout 20000 --exit"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.7.3"
  }
}
