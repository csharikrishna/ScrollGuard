# ScrollGuard Parental Control MVP — Audit Progress

Legend: `[ ]` pending · `[x]` resolved (Fixed / Confirmed-no-bug / Deferred-because-X, with file:line)

## Environment
- [x] JDK / gradlew toolchain resolution — Fixed-no-action-needed: JAVA_HOME=Android Studio JBR (JDK 21), `gradlew.bat --version` resolves cleanly (Gradle 8.4). `java`/`adb` not on PATH but not required since local.properties + JAVA_HOME already resolve.
- [x] Android SDK / adb / emulator or AVD availability — Confirmed present: SDK at %LOCALAPPDATA%\Android\Sdk, platform-tools/adb present, emulator present, TWO AVDs exist (Pixel_3a_API_35_extension_level_13_x86_64, scrollguard_test) — real emulator-based 2-device matrix testing is achievable.
- [x] firebase CLI + rules emulator + node/npm for rules test suite — firebase-tools 14.5.1, node v22.23.2, npm 10.9.8 all present. firebase.json has no "emulators" port config yet (needed before rules test suite) — to be added in testing phase.

## Part 0 — Non-negotiable invariants
- [ ] 1. Enforcement is local-only (zero network calls in BlockerAccessibilityService event path)
- [ ] 2. Offline continues working indefinitely
- [ ] 3. Config flows down / status flows up / enforcement reads local (no field ownership violation)
- [ ] 4. Existing functionality preserved (TimerState/TimerService/BlockerAccessibilityService/BlockActivity/PinActivity/app-selection/analytics/persistence/release config)
- [ ] 5. Parental-control state fully isolated from personal-timer state
- [ ] 6. Security boundary = Firebase Auth + Firestore Rules + App Check (google-services.json not a secret)
- [ ] 7. No per-second cloud traffic
- [ ] 8. Fail safe on every remote failure path

## Part 1 — Seed leads
- [x] 1. firestore.rules families Case 2 hijack (no affectedKeys on update) — CONFIRMED CRITICAL. Old rule: `isFamilyMember(familyId)` alone with zero field restriction let an already-paired child (or parent) rewrite parentUid/childUid to hijack the family. FIXED in firestore.rules: Case 2 now requires isChildOfFamily + parentUid/childUid unchanged + affectedKeys().hasOnly(['childDeviceName']). Also found and fixed a SECOND, worse variant during the same review: the `create` rule never checked `parentUid == null`, so a malicious child could self-assign an arbitrary parentUid at family-creation time, bypassing the pairing code mechanism entirely — fixed by adding that check to `allow create`.
- [x] 2. pairing/{code} allow read: if isAuthenticated() — CONFIRMED, and worse than described: this grants `list`/query access too (Firestore `read` = `get`+`list`), so any authenticated (incl. anonymous) client could enumerate ALL pending pairing codes across ALL families, no brute force needed. FIXED: split into `allow get` only (no `list` granted anywhere), preserving the legitimate single-code-fetch claim flow while making enumeration impossible. Also fixed a related gap: the `create` rule didn't validate the creator owns `familyId` as its child — fixed by requiring `get(.../families/$(familyId)).data.childUid == request.auth.uid` at create time.
- [x] 3. Pairing codes logged at Log.i in PairingManager.kt — CONFIRMED. Fixed: removed the `$code` value from both the generate and claim log lines (PairingManager.kt). Kept familyId in logs (low sensitivity — an unguessable 20-char Firestore auto-ID; the child already knows its own familyId from Room, and post-fix, knowing it grants no capability without the actual code).
- [x] 3b. NEW related finding (not in seed list): the OLD `config/current` write rule (`isParentOfFamily` only) made the entire pairing bootstrap flow non-functional once rules are enforced — PairingManager.generatePairingCode() has the CHILD create the initial disabled config/current stub while parentUid is still null, which isParentOfFamily always rejects. This is a load-bearing correctness bug, not just a security gap: it would have blocked pairing from ever working end-to-end. FIXED: split into `allow create` (child, only when `enabled == false`) + `allow update` (parent-only, unchanged).
- [ ] 4. No FirebaseMessagingService registered — CONFIRMED via manifest + full grep (zero hits for FirebaseMessagingService/onMessageReceived/onNewToken). firebase-messaging-ktx dependency is present but entirely unused. Actual propagation today = SyncWorker's 15-min WorkManager floor + whatever sync-on-open exists (being verified by UI/UX subagent) — NOT the spec's primary "parent write → FCM push → one read" path. Deciding fix scope — true push needs a Cloud Function trigger (server-side deploy to live Firebase project, needs user's go-ahead re: Blaze billing) — plan: implement client-side receiving plumbing now (FirebaseMessagingService + token registration), ask user before writing/deploying any Cloud Function. Not yet implemented — in progress.
- [ ] 5. No Firestore rules emulator test suite exists — CONFIRMED absent (no package.json/rules test files). firebase.json also has no "emulators" port config. To be built in testing phase (Part 4) — will also empirically verify the rules reasoning above (bootstrap fix, Case 2 fix, enumeration fix).
- [x] 6. app/google-services.json untracked but not gitignored — CONFIRMED. Verified contents are genuine public client config only (api_key is Android-package-restricted, no OAuth secrets) — consistent with invariant #6. FIXED: added `app/google-services.json` to .gitignore, added `app/google-services.json.sample` redacted template (matches existing keystore.properties.sample convention). README update still pending.
- [x] 7. ScrollGuardDatabase fallbackToDestructiveMigration + addMigrations(2_3,3_4) interaction; schema 3/4 json parity — CONFIRMED NO BUG for current v2/v3/v4 paths (Room always prefers a found migration path; fallback only applies to the disclosed, pre-parental-control v1 jump). Full column/type/nullability parity verified between MIGRATION_2_3/3_4 and schemas 3.json/4.json (only diff is Room schema JSON never capturing SQL DEFAULT clauses — a systemic Room characteristic, not an authoring bug). FIXED anyway as a latent-footgun hardening: changed `.fallbackToDestructiveMigration()` (unscoped — a future forgotten migration would silently wipe parental data too) to `.fallbackToDestructiveMigrationFrom(1)` (scoped to the actually-documented, actually-intended v1 jump) — ScrollGuardDatabase.kt.
- [x] 8. AdminReceiver exported; confirm not load-bearing for enforcement/pairing — CONFIRMED NO DEPENDENCY: AdminReceiver.kt only touches TimerState (pre-existing local feature); grep shows no DevicePolicyManager/AdminReceiver reference anywhere in parental-control code (PairingManager/SyncEngine/ParentalAuthManager/ParentalControlState all clean). exported=true is required by the DEVICE_ADMIN_ENABLED system contract (protected by BIND_DEVICE_ADMIN permission, not exploitable via export alone) — same reasoning applies to BlockerAccessibilityService (BIND_ACCESSIBILITY_SERVICE-protected) and BootReceiver (BOOT_COMPLETED is a protected broadcast AND BootReceiver.kt explicitly checks `intent.action == ACTION_BOOT_COMPLETED` before acting, so an exported-but-explicit-intent poke is a no-op). Play-policy risk of shipping Device Admin API in 2026 remains a valid Release-Audit note (not a code bug) — carried to Part 7 Release Audit.
- [ ] 9. Play Store policy exposure: BIND_ACCESSIBILITY_SERVICE + PACKAGE_USAGE_STATS + CAMERA + parental monitoring — to be assessed in final report Release Audit section (policy/compliance judgment call, not a code fix).

## Part 2 — Full audit scope (by area)
- [ ] Core/business logic (MVP feature completeness, state mgmt, race conditions, lifecycle)
- [ ] Data & sync (Room migrations, SyncEngine, SyncWorker, no write-echo/clobbering)
- [ ] Security & auth (ParentalAuthManager, PairingManager, firestore.rules, exported components, secrets, logging)
- [ ] Enforcement (BlockerAccessibilityService, BlockActivity, time math, tick loop, grace, day-rollover, reboot, self-block safety)
- [ ] Notifications & background (FCM, TimerService, SyncWorker constraints, service restart)
- [ ] Android platform (manifest, permissions, queries, lifecycle, insets, dark/light, localization)
- [ ] Performance (leaks, threading, battery/CPU, Firebase read/write cost)
- [ ] Release engineering (build.gradle signing, R8/ProGuard, CI, Play readiness)
- [ ] UI/UX (every screen states, steppers, animations, accessibility)

## Part 3 — Real-world condition matrix
- [ ] process death mid-tick
- [ ] Activity recreation mid-pairing/mid-dashboard
- [ ] AccessibilityService killed & restarted by OS
- [ ] device reboot
- [ ] network loss mid-sync
- [ ] network reconnection after extended offline
- [ ] Firebase write/read latency spikes
- [ ] duplicate/out-of-order Firestore updates (configVersion)
- [ ] rapid +/- tapping
- [ ] concurrent writes from parent and child to same document
- [ ] stale local config after long offline period
- [ ] corrupted/malformed Room or Firestore data
- [ ] device clock changed forward/backward
- [ ] timezone change (travel)
- [ ] midnight/day rollover while restricted app foregrounded
- [ ] USAGE_STATS/accessibility/camera permission revoked at runtime
- [ ] accessibility service disabled by user mid-session
- [ ] app updated (schema migration path)
- [ ] low-memory kill of app process
- [ ] background execution restricted by OS/OEM
- [ ] parent/child persistently out of sync
- [ ] child clears app data (re-pairing)
- [ ] two children/parents pair simultaneously / colliding codes

## Part 4 — Testing
- [ ] ./gradlew clean
- [ ] ./gradlew testDebugUnitTest
- [ ] ./gradlew lintDebug lintRelease
- [ ] ./gradlew assembleDebug
- [ ] ./gradlew assembleRelease (unsigned)
- [ ] Firestore rules emulator suite (build it — absent) — allow/deny matrix per Part E.3/H
- [ ] Manual/emulator walkthrough (Part 3 matrix) — environment-dependent
- [ ] Regression pass after fixes

## Part 5 — Grep sweep
- [ ] TODO/FIXME/HACK/XXX
- [ ] catch (Exception) / catch (Throwable)
- [ ] printStackTrace
- [ ] Log.d / Log.v / Log.i (sensitive payloads)
- [ ] !!
- [ ] @Suppress / @SuppressLint
- [ ] allow read, write: if true
- [ ] QUERY_ALL_PACKAGES
- [ ] affectedKeys usage in firestore.rules
- [ ] addSnapshotListener scoping
- [ ] hardcoded secrets outside google-services.json

## New findings from subagent tracks (enforcement / data-layer / release / UI)

### Enforcement & time-accounting (subagent track A) — all resolved
- [x] [HIGH] Day-rollover reset silently bypassed on hydration — `hydrateFromRoom()` stamped `currentEpochDay = today` unconditionally while loading each restriction's `consumedSeconds`/`consumedEpochDay` raw from Room with no day check, neutering `resetDayIfNeeded()`'s mismatch detection for the rest of that day. Failure scenario: reboot/service-restart at a moment Room holds yesterday's near-exhausted consumedSeconds → false "quota exhausted" block starts immediately with zero real usage that day, persisting until the next calendar day. FIXED: ParentalControlState.kt hydrateFromRoom now resets any restriction whose stored day != today (mirroring resetDayIfNeeded's own per-snapshot logic), gated by the same clock-tamper check (below) so a service restart can't launder a tampered clock into an early reset.
- [x] [HIGH] No dynamic launcher/home-package exclusion — EXCLUDED_PACKAGES was a static list with no launcher entry; device/OEM launcher package varies and isn't safe to hardcode. If a parent ever restricted a package that happens to be (or share identity with) the active launcher, the home screen itself would become unreachable with no way back to Settings/ScrollGuard (device-brick risk), directly violating spec Issue I. FIXED: BlockerAccessibilityService.onServiceConnected() now resolves the current CATEGORY_HOME package via PackageManager.resolveActivity() and registers it with ParentalControlState.registerLauncherPackage(); isAppQuotaExhausted/isParentallyRestricted/isExcludedFromBlocking all route through a shared isSelfOrSystemPackage() check that includes it.
- [x] [CONFIRMED GAP, spec Issue F] Clock-tamper detection was entirely unimplemented — day-rollover keyed purely on device wall-clock LocalDate.now() with no defense, so a child could open Settings (not blocked) and wind the clock backward to trivially reset consumedSeconds to 0 and grant unearned time. FIXED: added ParentalControlState.isClockTamperedBackward(context) — compares a persisted boot-time estimate (wall-clock minus elapsedRealtime, stable across one boot session) against the current one to distinguish a genuine reboot (elapsedRealtime legitimately resets) from a clock wound back with no matching reboot. Persisted in a dedicated SharedPreferences file (not Room — avoids a schema migration; kept separate from TimerState's own prefs per invariant #5). Wired into both resetDayIfNeeded() and hydrateFromRoom(). Local-only heuristic — does NOT do the spec's further-mentioned online serverTimestamp reconciliation; noted as a known limitation, not a full implementation of Issue F's "when online" refinement.
- [x] [LOW] incrementConsumed narrow race — already fixed earlier in this session (lookup moved inside the synchronized block).
- [ ] [LOW-MEDIUM, documented not fixed] Tick loop increments a flat +1 per Handler callback rather than measuring an actual elapsedRealtime() delta since the last tick (BlockerAccessibilityService.kt tickParentalTime/parentalTickRunnable). Deviates from the spec's literal mechanism but does NOT reintroduce double-counting or reboot-corruption (no elapsed baseline is persisted/compared across reboot) — net effect is a systematic small UNDERcount under scheduling jitter/GC pauses, i.e. fails in the safe direction. Deferred: real-world drift magnitude needs on-device measurement (Robolectric can't exercise real Handler/main-looper timing fidelity), and the fix is invasive relative to its benefit given it fails safe already. Recommend for a future pass.
- [x] Confirmed no bug (verified, not just assumed): zero network calls reachable from onAccessibilityEvent/checkAndBlockCurrentApp (invariant #1); no accessibility-event double-counting (select-vs-increment correctly separated, event subscription scoped to window-state/windows-changed only); allowance-change never resets consumed (primary path, plus unit-tested); remaining never stored/negative/overflow-prone; no stale elapsedRealtime persisted across reboot; day rollover correct while service stays alive continuously; grace period unit-consistent; block screen cannot accumulate/re-trigger parental time on itself; PinActivity covered by the com.scrollguard package exclusion; fail-safe on remote failure confirmed across pullConfig/pushStatus/pushCatalog/SyncWorker; OFF suspends without deleting; TimerState/ParentalControlState fully isolated (zero cross-references either direction, separate SharedPreferences vs. separate Room tables); DB migrations don't touch pre-existing tables.
- [ ] [NEEDS-VERIFICATION, documented] Brief fail-open window between onServiceConnected and hydrateFromRoom's async completion (isPaired/globalEnabled default false during that window → under-restricts, not over-restricts, so fails in the safe direction). Deferred — device-level timing measurement needed, low risk given fail-open direction.
- [ ] [NEEDS-VERIFICATION, documented] BlockActivity's parental-limit auto-dismiss rides TimerState.tickSignal, which only ticks while TimerService (the personal-timer engine) is actively running — a child using ONLY parental controls (never starting a personal focus session) would need to background/foreground the block screen to pick up a live allowance increase or midnight rollover, rather than auto-dismissing. Not a safety violation (no bypass), a UX responsiveness gap. Deferred — would need rewiring to the accessibility service's own tick rather than TimerState's, out of scope for this pass given no incorrect enforcement results.

### Data layer & migrations (subagent track B) — all resolved
- [x] CONFIRMED BUG (race condition): SyncEngine.pullConfig()'s old design read a Room snapshot, computed a merge in Kotlin, then blind-wrote it back — a concurrent BlockerAccessibilityService.flushPendingDeltasToRoom() (tick loop's ~15s batch persist) landing in that window would be durably applied then immediately clobbered, regressing consumedSeconds in Room (in-memory enforcement stayed protected via ParentalControlState's own maxOf-merge, so impact only surfaced across a process-restart/rehydrate boundary — a small unearned bonus quota). FIXED: replaced ParentalDao.replaceAllRestrictions (blind delete-all/insert-all) with ParentalDao.applyPulledConfig(incoming, todayEpochDay) — a single @Transaction that re-reads current Room state and decides per-row whether to preserve consumedSeconds (same day → update only parent-owned columns) or reset it (day changed/new package), so Room's own transaction serialization — not best-effort timing — is what prevents the race. SyncEngine.pullConfig updated to use it and to re-read Room afterward before refreshing the in-memory cache.
- [x] Confirmed no bug (verified): fallbackToDestructiveMigration/addMigrations interaction (see seed lead 7 above); full schema/migration column parity; same-database/separate-tables satisfies invariant 5 (zero cross-table queries either direction, no shared FK); no main-thread DB I/O anywhere; no AppDao/ParentalDao table or column collisions; unpair()'s non-atomic two-call Room cleanup can transiently orphan rows but they're enforcement-inert (isPaired gating) and self-heal on next sync.
- [ ] [Process gap, documented not fixed] No `androidTest`/`MigrationTestHelper` instrumented test exists to mechanically lock in migration/schema parity (currently verified only by manual diff). Recommend for follow-up; not blocking given the manual verification was thorough and the AVDs needed to run instrumented tests exist (noted for Part 4 testing phase — may add if time permits).

### Release engineering & CI (subagent track C) — all resolved
- [x] [CRITICAL] CI could almost certainly not build at all: app/google-services.json required by the google-services plugin was never committed (confirmed via `git ls-files`) and no CI step generated it — affects the entire build job (assembleDebug/Release, and likely testDebugUnitTest/lintDebug too since includeAndroidResources=true triggers the same resource processing). This is a pre-existing gap independent of my earlier .gitignore fix, but that fix would have made it permanent (git would never auto-sweep the file in via a future `git add -A` either). FIXED: added a "Write google-services.json" step (from a new GOOGLE_SERVICES_JSON_BASE64 secret, fails loudly if absent) to both the `build` and `release` jobs in android-ci.yml; documented the new required secret and local setup (google-services.json.sample) in README.
- [x] [CONFIRMED BUG] No R8 keep rule for SyncWorker — WorkManager instantiates Workers reflectively by class name (from its own internal work database), which R8 can't trace; the existing proguard-rules.pro only had a keep rule for `com.scrollguard.data.**`, not the sibling `com.scrollguard.parental.**` package SyncWorker lives in. minifyEnabled=true for release, so this was invisible in debug and would only surface as a silently-broken production safety-net sync. FIXED: added `-keep class com.scrollguard.parental.** { *; }`.
- [x] [HIGH] Tag-triggered signed-release CI job could silently produce an UNSIGNED artifact and still report success: the "Decode release keystore" step only checked RELEASE_KEYSTORE_BASE64 was non-empty, not the other 3 required secrets (RELEASE_STORE_PASSWORD/KEY_ALIAS/KEY_PASSWORD) — if any of those were missing/misnamed, app/build.gradle's (correct, for local dev) unsigned-fallback design would kick in silently, and the upload-artifact step's default `if-no-files-found: warn` would let the job go green even though app-release.apk (the signed name) would never exist (only app-release-unsigned.apk would). FIXED: the keystore-decode step now checks all four secrets and fails loudly if any are missing; the upload step now sets `if-no-files-found: error` as defense in depth.
- [x] Confirmed no bug (verified): .gitignore core coverage (keystore.properties/*.jks/*.keystore/local.properties/.gradle//build/) all correct; no committed secrets repo-wide beyond the expected (non-secret, per invariant #6) Firebase client API key; no tracked .jks/.keystore; CI trigger config correct (push+PR to main, zero continue-on-error anywhere); CI/Gradle signing env-var names match exactly; root build.gradle google-services plugin version compatible with firebase-bom; no Firestore .toObject()/reflection risk (SyncEngine uses only raw getBoolean/getLong/getString + maps, so no @Keep gap there); Lottie/MPAndroidChart proguard rules present and previously verified; Room parental entities already covered by existing data.** keep rule; manifest-declared components need no explicit keep rules (AGP auto-generates them, a different mechanism from WorkManager's reflection).
- [ ] [NEEDS-VERIFICATION, deferred to Part 4 testing] zxing/zxing-android-embedded R8 coverage not yet empirically confirmed (no explicit rule exists; convention is these AARs bundle adequate consumer rules, but this project's own established practice per proguard-rules.pro's header is to verify via a real assembleRelease + missing_rules.txt inspection) — will check when I run the Part 4 build commands.

### UI/UX (subagent track D) — triaged below, fix pass in progress
See "UI/UX findings" section below for the full list and fix/defer decisions.

## New findings beyond the seed leads (found during static read, before subagent tracks launched)
- [x] ParentalControlState.refreshFromSync day-boundary bug — the "preserve fresher local consumedSeconds" max-merge compared raw values without checking `oldSnap.consumedEpochDay == r.consumedEpochDay`, so a stale in-memory count from before midnight (if resetDayIfNeeded() hadn't run yet in-memory) could survive a sync refresh into the new day via max(). FIXED: ParentalControlState.kt refreshFromSync now only takes the max when the old snapshot is from the same accounting day; otherwise trusts the incoming (already-correctly-reset) value.
- [x] SyncWorker cold-process hydration gap — doWork() checked `ParentalControlState.isPaired` (in-memory singleton, defaults false) without ever hydrating from Room first. If WorkManager starts the worker in a fresh process (e.g. after the app process was killed and the accessibility service hasn't reconnected yet), the safety-net sync would silently no-op exactly in the process-death scenario it exists to recover from. FIXED: SyncWorker.doWork() now calls ParentalControlState.hydrateFromRoom(dao) unconditionally before checking isPaired/role.
- [x] ParentalControlState.incrementConsumed narrow race — map lookup happened outside the synchronized block that guards restrictions-map swaps, so a concurrent hydrateFromRoom/refreshFromSync landing between the lookup and the increment could increment an orphaned snapshot instance, silently losing that tick's second. Low real-world impact (at most ~1s lost occasionally) but cheap to fix — FIXED: lookup moved inside the same synchronized block.
- [ ] SyncEngine writeGlobalEnabled/writeAppRestriction/removeAppRestriction each do 2 sequential non-atomic Firestore writes (app doc + configVersion bump on config/current) instead of a WriteBatch — a crash/network-drop between the two calls leaves configVersion slightly wrong, though the child's full-reread pull model means it doesn't actually desync the real config values. Medium severity, not yet fixed — candidate for fix pass.
- [ ] Undocumented "time requests" feature (BlockActivity.kt timeRequestListener + firestore.rules /requests subcollection) exists but is NOT part of ScrollGuard_Parental_Control_MVP.md's spec at all (spec Part I explicitly says keep MVP focused, no complex dashboards). Not a security bug (parent is already the trusted authority; no affectedKeys gap here is exploitable beyond what parent already controls) but is scope creep beyond the authoritative spec — flag in MVP Feature Audit rather than fix.

## UI/UX findings (subagent track D) — fix/defer decisions

FIXED (all 9 items below landed in ParentalControlActivity.kt / ParentalAppAdapter.kt / layout+string files):
- [x] Fake/cosmetic "Connected" status — shown regardless of lastSeen staleness (ParentalControlActivity.kt dashboardStatusListener), directly contradicts spec's explicit "no misleading connection status." Fix: compute staleness client-side from the lastSeen timestamp already being read; use the existing-but-unused `child_offline`/`child_last_seen_format` strings.
- [x] Static "Connected"/"Child Device" layout defaults shown before any listener returns real data (activity_parental_control.xml defaults + showParentDashboard timing) — pair with the fix above (start from a neutral/loading label, not an optimistic default).
- [x] Pairing code lost permanently on rotation (ParentalControlActivity.kt showChildPairingView — code only ever held as a function parameter, never persisted) — fix via onSaveInstanceState (contained; doesn't require Room schema changes, matches the specific reported bug which is rotation, not process-death).
- [x] Stepper +/- has no optimistic UI update and no debounce, so rapid taps net fewer increments than taps and the last write silently "wins" — directly contradicts spec Part G's explicit "immediate optimistic UI... offline changes queue and reconcile." Fix: update the adapter's bound item immediately on tap, and coalesce/replace in-flight writes per package.
- [x] switchEnabled (per-app toggle) has no contentDescription — cheap, direct a11y fix.
- [x] Touch targets below 48dp minimum (item_parental_app.xml btnMinus/btnPlus 40dp; item_app_group.xml btnEdit/btnDelete 36dp) — cheap XML fix.
- [x] dialog_edit_group.xml steppers have no contentDescription at all, regressing a pattern activity_main.xml already established for the pre-existing steppers — cheap XML fix, mirror the existing convention.
- [x] Returning already-paired child re-opening Parental Control never calls pullConfig()/pushStatus() (ParentalControlActivity.kt goes straight to showChildStatus) — misses the spec's explicitly-required sync-on-app-open fallback, compounding the no-FCM gap (seed lead 4) since this was supposed to be one of the two fallback paths and it's also missing. Fix: call syncEngine.pullConfig()/pushStatus() when the child screen is shown.
- [x] [MEDIUM-HIGH, from enforcement track] Parent "Approve time request" reads ParentalControlState.getRestriction() for the current allowance, which is only ever populated on CHILD-role devices (refreshFromSync is never called on the parent) — so on the parent device this is always null, silently falling back to a hardcoded 3600s, meaning approving a request OVERWRITES the real allowance instead of adding to it. Fix: read the current allowance the same way the working +/- stepper path does (a fresh Firestore read), not from ParentalControlState.

DEFERRING (documented, with reason) — will appear in the final report rather than being fixed in this pass:
- [ ] syncState hardcoded to "SYNCED" on every write (SyncEngine.pushStatus, PairingManager family creation) — the child never actually reports OFFLINE/STALE. The client-side staleness-from-lastSeen fix above mitigates the main symptom (misleading "Connected") without needing this, but a fully honest syncState would need the child to detect its OWN connectivity state and write it, which is a larger change. Deferred — recommend for follow-up.
- [ ] No Error UI state ever reachable — all ~5 Firestore listeners discard their error parameter, no failure listener on the one-shot family-doc read, error_sync_failed string unused. Full fix needs an error-state UI branch wired through every listener. Given the number of call sites (5+) and that none of them currently have ANY error handling to build on, doing this properly is a moderate-sized addition — deferring to keep this pass's remaining budget for other fixes, flagging as a High-severity finding for the report since it's spec-explicit ("Error state: cause + action").
- [ ] Reactive gap: no live path back to "No child connected" if unpaired mid-session from the other device — requires either a listener on the family doc itself or periodic re-check; deferred as a lower-frequency edge case.
- [ ] Dead-end pre-pairing child screen (no back/role-reset affordance before pairing completes) — a real UX gap but requires new UI (a back/reset button + confirmation) rather than a logic fix; deferred, recommend for follow-up.
- [ ] performClaimPairingCode rides lifecycleScope — rotation mid-claim can let the server-side pairing succeed while the local Room write/UI transition never completes, desyncing local state from Firestore with no automatic recovery. Real architectural gap (a one-shot critical mutation shouldn't be tied to Activity lifecycle) but fixing it properly means restructuring where that coroutine lives (e.g. giving PairingManager its own outlasting scope) — deferred given the risk of a rushed change to pairing-claim code, flagging as High-severity for the report with the specific recommended fix.
- [ ] Catalog-not-synced / empty / fetch-error all show the identical "No apps found" message (ParentalAppPickerActivity + SyncEngine.readChildCatalog collapsing Result.failure into emptyList()) — deferred, moderate effort to thread a distinct state through; flagging for follow-up.
- [ ] Existing-restrictions fetch silently swallows failure to emptySet() (same file) — same reasoning, deferred alongside the item above.
- [ ] Approve/deny buttons on time requests have no double-tap guard — minor, same root cause as the stepper debounce fix; deferred as lower severity (a duplicate approve is a minor annoyance, not data corruption, once the allowance-overwrite bug above is fixed).
- [ ] Hardcoded (non-@string/) strings in new Kotlin/layout files (~10 call sites: Toasts in ParentalControlActivity/ParentalAppPickerActivity/AppGroupsActivity, 2 contentDescriptions, dialog strings) — mechanical but numerous; deferring the exhaustive conversion, will fix the ones that overlap with other fixes already in progress (e.g. contentDescriptions touched for the a11y fixes above) and list the rest for follow-up.
- [ ] unpair() doesn't clean up Firestore subcollections (config/status/catalog/apps/requests) — already decided/documented above as a low-severity, explicitly-deferred data-hygiene item.
- [ ] No FCM (seed lead 4) — client-side receiving plumbing still pending; full fix needs a Cloud Function deploy decision from the user.

## Decisions / deferred-by-design (not bugs, scope calls made during the audit)
- Orphaned Firestore subcollection docs after unpair (config/current, status/current, catalog/current, apps/*, requests/*) are NOT recursively deleted by PairingManager.unpair() (which only deletes the top-level family doc — Firestore never cascade-deletes subcollections). Decided NOT to add delete rules + client-side recursive cleanup for this: it's a low-severity storage/hygiene cost (orphaned docs are unreachable and inert, not a security hole), and doing it properly needs either a Cloud Function or non-trivial client-side collection listing+batch-delete, which is disproportionate complexity for a low-severity item. Documented as a known limitation in the final report instead.
- FCM Cloud Function trigger (server-side, would need deployment to the live Firebase project `scrollguard-aba84` and Blaze billing plan) is being treated as a decision requiring explicit user go-ahead, not something to deploy unilaterally — consistent with treating deploys to shared/live infrastructure as needing confirmation. Client-side receiving plumbing (FirebaseMessagingService, token storage) is being implemented regardless since it's zero-risk/inert until a trigger exists.

## Notes / Assumptions Log
(populated as ambiguities are resolved)
